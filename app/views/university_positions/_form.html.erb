<%= form_with model: @university_position, local: true do |form| %>

  <div class="form-group row">
    <%= form.label :name, 'Nombre:', class: 'col col-form-label' %>
    <div class="col-9">
      <%= form.text_field :name, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :per_travel_payment, 'Importe de Viáticos:', class: 'col col-form-label' %>
    <div class="col-9">
      <%= form.text_field :per_travel_payment, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :per_lunch_payment, 'Importe de Almuerzos:', class: 'col col-form-label' %>
    <div class="col-9">
      <%= form.text_field :per_lunch_payment, class: 'form-control' %>
    </div>
  </div>

  <p class="text-right">
    <%= link_to "Cancelar", :back, class: 'btn btn-outline-secondary' %>
    <%= form.submit 'Guardar', class: 'btn btn-success' %>
  </p>
<% end %>