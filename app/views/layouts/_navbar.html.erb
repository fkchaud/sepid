<nav class="navbar navbar-expand fixed-top navbar-dark bg-primary" style="background-color:#CDD3CE;">
  <a class="navbar-brand" href="/">SePID</a>
  <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <% if current_user %>
        <li class="nav-item active dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="dropdownAccess" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.user_name %>
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownAccess">
            <%= link_to 'Cerrar sesión', logout_path(current_user), method: :delete , class: "dropdown-item"%>
            <%= link_to 'Modificar perfil', edit_user_path(current_user), class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li class="nav-item active">
          <%= link_to 'Iniciar sesión', login_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>