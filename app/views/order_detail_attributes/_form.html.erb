<%= form_with scope: :order_detail_attribute, url: order_type_order_detail_attribute_path, method: "patch", local: true do |form| %>

  <div class="form-group row">
    <%= form.label :attribute_name, 'Nombre:', class: 'col col-form-label' %>
    <div class="col-10">
      <%= form.text_field :attribute_name, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group row">
    <%= form.label :attribute_type, 'Descripción:', class: 'col col-form-label' %>
    <div class="col-10">
      <%= form.text_area :attribute_type, class: 'form-control' %>
    </div>
  </div>

  <p class="text-right">
    <%= link_to "Cancelar", :back, class: 'btn btn-outline-secondary' %>
    <%= form.submit 'Guardar', class: 'btn btn-success' %>
  </p>
<% end %>