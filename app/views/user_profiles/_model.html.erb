<%= form_with model: @user_profile, local: true do |form| %>

  <div class="form-group row">
    <%= form.label :name, 'Nombre:', class: 'col-sm col-form-label' %>
    <div class="col-sm-10">
      <%= form.text_field :name, class: 'form-control-plaintext', readonly: true %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :description, 'Descripción:', class: 'col col-form-label' %>
    <div class="col-10">
      <%= form.text_area :description, class: 'form-control-plaintext', readonly: true %>
    </div>
  </div>

  <div class="form-group row">
    <label class="col">Permisos:</label>
    <div class="col-10">
      <%= form.collection_check_boxes :access_permit_ids, AccessPermit.all, :id, :use_case_name do |cb| %>
        <div class="form-check">
          <%= cb.check_box disabled: true%>
          <%= cb.label disabled: true%>
        </div>
      <% end %>
    </div>
  </div>

<% end %>